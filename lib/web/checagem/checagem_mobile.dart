import 'dart:async';
import 'state_bloc.dart';

class ChecagemWebMobile {
  Stream<WebUserStatus> checarUsuarioWeb() {
    // Substituir com a lógica para dispositivos móveis, se necessário
    return  const Stream.empty();
  }
}
